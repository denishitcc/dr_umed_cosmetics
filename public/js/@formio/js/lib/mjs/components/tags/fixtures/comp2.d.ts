declare namespace _default {
    let label: string;
    let tableView: boolean;
    let maxTags: number;
    let calculateServer: boolean;
    let key: string;
    let type: string;
    let input: boolean;
}
export default _default;
