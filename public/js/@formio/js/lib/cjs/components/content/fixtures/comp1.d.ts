declare namespace _default {
    namespace conditional {
        let eq: string;
        let when: null;
        let show: string;
    }
    let tags: never[];
    let type: string;
    let html: string;
    let input: boolean;
    let key: string;
}
export default _default;
