declare namespace _default {
    let label: string;
    let optionsLabelPosition: string;
    let inline: boolean;
    let tableView: boolean;
    let defaultValue: boolean;
    let values: {
        label: string;
        value: string;
        shortcut: string;
    }[];
    namespace validate {
        let onlyAvailableItems: boolean;
    }
    let key: string;
    let type: string;
    let input: boolean;
}
export default _default;
